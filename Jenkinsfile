pipeline {
    agent any
    stages {
        stage('Check Connection') {
            steps {
                echo "Jenkins is connected to GitHub repository successfully!"
            }
        }
    }
}
